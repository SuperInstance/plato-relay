[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-relay"
version = "0.1.0"
description = "Message relay with tidepool buffering and cross-agent delivery"
license = {text = "MIT"}
requires-python = ">=3.8"
